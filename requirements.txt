fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
python-dotenv>=1.0.1
PyYAML>=6.0.1
numpy>=1.26.0,<2
# --- embeddings ---
openai>=1.40.0
# voyageai>=0.2.0            # uncomment if you set embedding.provider: voyage
# tiktoken>=0.7.0            # optional: token-accurate chunking (auto-fallback otherwise)
# --- parsing ---
pypdf>=4.2.0
python-docx>=1.1.0
# --- vector store ---
# chromadb>=0.5.0            # optional: backend "chroma" (auto-fallback to "local")